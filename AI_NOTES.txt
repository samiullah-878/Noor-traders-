AI_NOTES.txt — Sam Blue Khata / Noor Traders
Kaam ka khulasa (doosre AI / developer ke liye). Har update ke saath is file mein naya hissa jorein.
NOTE: Yeh repository PUBLIC hai. Is file mein koi password, key, account number ya ID na likhein.

==============================================================
SYSTEM KA NAQSHA
==============================================================
1) Mobile/Web app (yeh repository, GitHub Pages)
   - Firebase project: note-traders-khata-7ccc1 (Blaze plan, $5 budget alert laga hai)
   - Business id: noor-traders
   - Stock tab ka code: pos-stock.js (+ style.css)
   - Version badalne par yeh sab jagah version number badalta hai:
     app.js, cloud.js, index.html, sw.js, version.json

2) Shop PC (Windows, folder C:\khata-sync) — Node.js scripts, SQL Server Express (database POS)
   Chaar loops hamesha chalte hain (har loop .bat se, band ho to khud dobara):
   - khata-sync-auto.bat -> pos-khata-sync.js --auto  (app <-> POS accounts/vouchers, rasid print)
   - farq-auto.bat       -> post-farq.js --auto       (stock ginti farq bill, "Band" items)
   - stock-auto.bat      -> sync-stock.js             (POS stock -> app, har 2 minute)
   - bills-auto.bat      -> sync-bills.js             (purchase bills -> app, har 2 minute)
   - khata-auto.bat (purana sync.js) start-all mein BAND hai.
   - SQL ki settings sirf sync-stock.js ke SQL_CONFIG mein; baqi scripts wahin se parhti hain.
   - Firebase key: C:\khata-sync\firebase-key.json (repo mein kabhi na daalein)

==============================================================
2026-09-17 — KIYA GAYA KAAM
==============================================================
PC restart / auto start
- SQL Server (MSSQL$SQLEXPRESS) Automatic + band ho to khud restart.
- Task Scheduler: "KhataSync-StartAll" (Dell user login ke 1 minute baad) -> C:\khata-sync\start-all.ps1
  start-all.ps1 SQL ka intezar karta hai aur har loop sirf tab chalata hai jab woh pehle se na chal raha ho
  (double entry se bachao). Log: start-all-log.txt
- Purana task "Khata Sync" Disabled hai. Startup folder mein purani .vbs files hain (masla nahi).
- Printer: Windows ka "default printer khud badalna" band kiya (LegacyDefaultPrinterMode=1);
  rasid default printer par jati hai (notepad /p).

Firebase quota
- 16 Sep ko reads 68K (free had 50K) -> "Quota exceeded". Wajah: post-farq har 20 second poori ginti list parhta tha.
- Hal: post-farq ab listeners istemal karta hai (v10). Blaze plan on kiya.

pos-khata-sync.js
- App mein entry save hote hi 3 second mein POS voucher (pehle 60 second). 60 second loop ab bhi ehtiyat ke liye.
- App se entry delete -> POS voucher delete + "ENTRY DELETE HO GAYI" rasid print + app doc par posDeletedAt.
- "khata-sync.pause" file ho to sync ruka rehta hai (aaj yahi masla tha, file delete ki).

post-farq.js (v9 — NOTE: yahan pehle galti se v10 likha tha)
- Is din sirf _hidden aur ginti (round) par listener laga; _round aur _post abhi get() se parhe jate the.
  Asal v10 neeche 2026-09-17 (shaam) mein hai.
- "Band" feature: app mein Band tick -> POS mein stock 0 (stock fraq ka sale/return bill, khareed bhao par)
  + item INACTIVE + "ITEM BAND - STOCK 0" rasid. Tick hataya -> item dobara ACTIVE (stock wapas nahi).
  Feature se pehle ke band items ko nahi chheda jata (band-state.json mein "old").
  Purane band items: node post-farq.js --band-old  (list)  /  --band-old --yes  (karo)
- Aaj band-old ke 2 "–" items ka kaam chalaya gaya.

sync-stock.js
- INCLUDE_ZERO = true  (zero stock items app mein aate hain)
- SKIP_INACTIVE = true (POS mein inactive items aur jin ka naam sirf "-"/"–"/khali hai, app mein nahi aate)

items-clean.js
- "–" naam wale / inactive-magar-stock-wale items ka stock 0 + inactive. Aaj list khali thi (kaam pehle ho chuka).

App (pos-stock.js) versions
- v1.45.1: Stock tab ke buttons hisson mein (Branch / Dikhao / Ginti / Tarteeb).
- v1.45.2: Tez: Intl.NumberFormat ek dafa banaya (pehle har number par naya — bohat slow),
           Collator se sort, 30 rows ek dafa, content-visibility. Buttons chhote (3 ek qatar mein).
- v1.45.3: Ginti 0 save karne par confirm; Band tick lagane/hatane par confirm (cancel par tick wapas).
- v1.45.4: "📷 Barcode scan karein" (Android Chrome BarcodeDetector). Scan -> item search mein,
           peeli highlight, cursor Ctn (carton wale) ya Kul Pcs par. Save ke baad agla scan khud (option,
           localStorage "stockScanAgain"). Code ItemCode se milta hai, shuru ke zero nazar-andaz.

Check karne ki commands (PC, CMD)
- Loops:   powershell -c "gcim Win32_Process | ? Name -eq 'node.exe' | % CommandLine"
- Logs:    powershell -c "gc C:\khata-sync\khata-sync-log.txt -Tail 10"   (farq-log.txt, stock-log.txt, bills-log.txt)
- Printer: powershell -c "gcim Win32_Printer | ? Default | % Name"

Baqi / yaad rakhein
- Kal Firebase Usage and billing mein 17 Sep ki reads dekhni hain (50K se kam honi chahiyein).
- Purani backup files PC par: post-farq.old.js, post-farq.v2old.js, pos-khata-sync.old.js, sync-stock.old.js, sync-stock.v2old.js

==============================================================
2026-09-17 (baad mein) — v1.45.5: POS ke saare barcode
==============================================================
- POS item screen mein "SB Barcode" grid hai: ek item ke kai barcode (Qty, Sale Rate ke saath).
- sync-stock.js: table khud dhoondta hai (naam mein "barcode" + ItemID khana, ya Items ka barcode khana)
  aur har item ke baqi barcode `bc: [...]` mein app ko bhejta hai (ItemCode `code` mein hi rehta hai).
  Pehli run par log mein likhta hai: "Barcode khane: <table>.<khana>".
- pos-stock.js: scan aur search dono ab `code` + `bc` mein dhoondte hain (shuru ke zero nazar-andaz).
- Abhi barcode ki Qty (maslan carton barcode = 12) istemal nahi hoti; scan sirf item tak le jata hai.
- POS mein barcode table: ItemSubCode, khana SBBarCode (1076 active items mein se 788 ka ItemCode khud barcode hai).
  sync-stock.js ab "%subcode%" wali table bhi dekhta hai (stock-barcode-v4).

==============================================================
2026-09-17 — v1.45.6: kai items ek sath scan (scan list)
==============================================================
- Camera khula rehta hai; har scan item ko `scanList` mein daalta hai (vibrate, aakhri 4 naam nazar aate hain).
  Wahi barcode 2.5 second tak dobara nahi gina jata; pehle se list wala item dobara nahi judta.
- "✓ Ho gaya" -> screen par SIRF scan wale items (scan ki tarteeb mein), pehle item ke khane par cursor.
- Enter = agla ginti khana. "💾 Sab save karein" -> jin items ki ginti likhi/badli hai sab ek confirm ke saath save
  (ZERO walon ka alag zikr). Khali khanon wale items chhor diye jate hain.
- "✕ Saaf karein" -> scanList khali, search khali, wapas poori list. Phir "📷" se naya scan.
- Purana "save ke baad agla scan khud khule" option hata diya (is naye tareeqe ki wajah se).
- Code: pos-stock.js — readCount / writeCount / saveCount / saveAll / addScanned / findByCode / finishScan / clearScan.

==============================================================
2026-09-17 — v1.45.7: USB / Bluetooth barcode scanner
==============================================================
- Hardware scanner keyboard ki tarah likhta hai. pos-stock.js keydown (capture) par tez akshar pakadta hai:
  kam az kam 6 akshar, ausat faasla < 35ms, phir Enter/Tab (ya 120ms khamoshi) = barcode.
  Jo akshar kisi khane (ginti/search) mein chale gaye woh wapas hata diye jate hain.
- Barcode -> addScanned() -> scan list -> screen dobara -> us item ke ginti khane par cursor.
- Insaan ka likhna (dheema) is se mutassir nahi hota. Camera khula ho to yeh band rehta hai.
- Screen dobara banne par likhi hui (unsaved) ginti ab gum nahi hoti (captureTyped/restoreTyped).
  "Saaf karein" likhi hui ginti bhi saaf karta hai.
- Mobile par: USB-C OTG adapter (USB-C male -> USB-A female) se scanner lagta hai. Kuch bare
  (omni/table) scanner ko zyada bijli chahiye; na chale to powered OTG hub.
- PC browser mein bhi yahi scanner isi tarah kaam karta hai.

==============================================================
2026-09-17 (shaam) — PC scripts ki jaanch aur 4 islahat (app files nahi badli)
==============================================================
Jaanch mein mila:
- PC par post-farq v9 chal raha tha: _round aur _post har 20 second get() = roz ~8,600 Firebase reads.
- pos-khata-sync ka listener shuru ke baad toote (maslan quota) to script purane data par chalti rehti thi,
  app ki entries POS mein jaana chup chaap band ho jata.
- sync-bills POS mein delete hue bill app se kabhi nahi hatata tha.
- sync-stock / sync-bills: setInterval par koi rok nahi thi; lamba chakkar agle chakkar se SQL connection takra sakta tha.

Islahat (C:\khata-sync, LAGAO.bat se lagi; purani files *.v3old.js):
- post-farq.js v10: _round aur _post par onSnapshot (watchCtl). _post badalte hi 1.5 second mein tick (kickFarq).
  SQL wala kaam (sysPos, Band) ab bhi har 20 second — us mein Firebase read nahi.
  Tick ab "again" ke saath (beech mein hukam aaye to chakkar dobara). "posting" ka taala transaction mein taaza parhta hai.
  _round/_post/_hidden listener toote -> process.exit(1) -> farq-auto.bat 30 second mein dobara.
- pos-khata-sync.js v7: app aur links listener shuru ke baad toote -> process.exit(1) -> bat 60 second mein dobara.
- sync-bills.js: jo "pos-" bill pehle likha tha aur ab SQL mein nahi -> Firestore se delete. Script shuru par ek dafa
  posBills (date >= START_DATE, sirf id) se milaan. MAX_DELETE = 20: is se zyada ek sath gayab hon to kuch nahi mitata
  (log: "KHABARDAR"). SQL se 0 bill aayen to kuch nahi mitata. Log mein ab "hataye N".
- sync-stock.js / sync-bills.js: busy rok — pichla chakkar chal raha ho to naya chhor deta hai.
- post-farq-NEW.js (post-farq.js ki bilkul copy) delete ki.

Firebase ka andaza (roz):
- sync-stock: poora milaan ghante mein 1 = ~430 reads; writes sirf badle chunk + meta.
- sync-bills: reads sirf script shuru par (bill ginti ke barabar); writes sirf naya/badla bill.
- pos-khata-sync: shuru par poora khata ek dafa, phir sirf tabdeeli.
- post-farq: ab sirf tabdeeli ke reads (pehle ~8,600 fixed).
- Yaad rahe: har PC write, har khule mobile ke liye 1 read bhi banta hai (app listeners).

Baqi / yaad rakhein
- SQL password kai one-time scripts mein seedha likha hai; asal jagah sirf sync-stock.js SQL_CONFIG honi chahiye.
- Kal Firebase Usage mein reads dobara dekhein; post-farq wale ~8,600 kam nazar aane chahiyein.

==============================================================
2026-09-17 (shaam) — App se sale (counter + wholesale): tayyari
==============================================================
- Malik chahta hai app se sale daale: counter sale (cash, walk-in) aur wholesale (party ke khate mein udhaar).
- Mansooba: app "Nayi Sale" -> Firestore -> PC script (post-farq wala usp_Sale_InsertUpdate tareeqa) -> POS bill + rasid.
- Pehla qadam: C:\khata-sync\sale-dekho.js (sirf parhta hai) -> sale-dekho.json. Is se pata karna hai:
  cash sale ka party/IsCreditSale/CashReceived, wholesale rate alag khana hai ya nahi, voucher kaise bante hain.
- post-farq.js --print <SaleNo> se farq bill ki rasid dobara print hoti hai (pehle se mojood).

==============================================================
2026-09-17 — v1.45.8: Farq bills ki history alag khirki mein
==============================================================
- Masla: Stock tab mein "Farq bills ki history" khulti to header (summary) lamba ho jata aur search box bohat neeche chala jata.
- Hal (pos-stock.js): history ab inline nahi, app ke #dialog mein khulti hai (openBills). Button: "📋 Farq bills ki history (N)".
  showBills wala state hata diya. × se band.
- Version 1.45.8: app.js, cloud.js, index.html, sw.js, version.json.

==============================================================
2026-09-17 — Stock rate ka farq (app vs POS item screen)
==============================================================
- Misal kala chana (000033, pack 25): POS screen Purchase 5685.05/ctn (=227.40/pc), R Peice 260, W Peice 237,
  R Cotton 6500, W Cotton 5925, Average 199.974. App dikha raha tha Rate 235, Khareed 209.51.
- Wajah: sync-stock.js rate Items.SaleRate / Items.PurchaseRate se leta hai (purane/alag khane).
  POS screen ke rate shayad ItemBranchRate ya R/W ke alag khanon mein hain.
- POS mein Retail (R) aur Wholesale (W) rate alag hain -> Nayi Sale: counter = R, wholesale = W.
- Tasdeeq ke liye rate-dekho.js (sirf parhta hai): node rate-dekho.js 000033 -> rate-dekho.json.
  Us ke baad sync-stock.js mein sahi khane lagane hain (app mein R aur W dono dikhane ka irada).

==============================================================
2026-09-17 — v1.45.9 + sync-stock rate v2: sahi R / W / Khareed rate
==============================================================
- rate-dekho.js se pakka hua: POS item screen ke rate dbo.ItemBranchRate mein hain (har branch ki row):
  SaleRate = R Peice (260), SaleRate2 = 260, SaleRate3 = 237, SaleRateSize = 237 (W Peice),
  SaleRate2Size = 0, PurchaseRate = 227.402 (khareed, pehle se FI PIECE), PackQty = 25.
  (SaleRate3 aur SaleRateSize mein se asal W kaun sa hai, abhi pakka nahi — dono 237.)
- sync-stock.js: Q_STOCK ab r.SaleRate (BRetail), r.SaleRate3 (BWhole), r.SaleRateSize (BWholeSize), r.PurchaseRate (BCost) bhi parhta hai.
  rate = BRetail (warna Items.SaleRate), naya khana wrate = BWhole || BWholeSize,
  prate = BCost agar > 0 (warna purana tareeqa: Items.PurchaseRate / aakhri bill).
  Pehle Items.SaleRate/PurchaseRate se aata tha (kala chana: 235 / 209.51 — galat).
  LAGAO-RATE.bat: backup sync-stock.v4old.js, last-stock.json delete (saara stock dobara jata hai, ek dafa ~20 writes).
- App pos-stock.js: item line mein "R 260 · W 237 · Khareed 227.4" (pehle "Rate · Khareed").
- post-farq.js pehle se ItemBranchRate.PurchaseRate istemal karta hai (theek).
- Nayi Sale ke liye: counter = rate (R), wholesale = wrate (W).
- Nazar aaya: kala chana branch 10 mein +3,941, doosri branch mein -3,333 (maal ek godam, bikri doosre se).

==============================================================
2026-09-17 — Nayi Sale: malik ke faisle + purchase bill ke 2 masle
==============================================================
Nayi Sale (abhi bani nahi):
- Taala (trg_SaleAllowedParty) RAHEGA. Sale sirf COUNTER SALE (PartyID 1) aur "whole sale (purana)" (PartyID 21629) par.
- Default Counter (R = ItemBranchRate.SaleRate, cash, CashReceived = total).
- Wholesale button: W rate (SaleRate3/SaleRateSize), udhaar sale + usi waqt cash ka CRV (POS jaisa; payment khud total, badal sakte hain).
- Malik rate badal sakta hai, mulazim fix rate. Nayi login scope "sale" (Sirf Sale) jo sirf Sale screen khole.
- POS dobara save (F5): usp_Sale_InsertUpdate CancelAndNew=1 -> purani sale DocStatusID 3 (cancel) + nayi SaleNo. Double nahi hota.
- Rukawat: repo ki firestore.rules purani hai (stockCount ka zikr nahi) -> Firebase console ke asal rules chahiyein.

Purchase bill (app "Bill dekhein"):
- Entry "POS Purchase 00002446 Rs 3,780" par bill 00002443 bismillah traders Rs 465,499.99 khula (id pos-<PurchaseID> milta nahi).
- Qty: Lifebuoy shampoo 50 ctn (= 600 pcs, pack 12) app mein "600 CTN = 7200 PCS" dikha.
  Andaza: PurchaseDetail.Qty PIECES mein hai aur Rate FI PIECE (sync-bills.js ise carton samajhta hai).
- sync-bills.js cancelled (DocStatusID 3) purchase bhi bhejta hai (sync.js SKIP_STATUS = [3] chhorta tha).
- Tasdeeq: bill-dekho.js (sirf parhta hai): node bill-dekho.js 00002443 00002446 -> bill-dekho.json.

==============================================================
2026-09-17 — v1.46.0: NAYI SALE (Counter / Wholesale) + "Sirf Sale" login
==============================================================
Firebase rules (firestore.rules) — PEHLE console mein Publish karein:
- Base: Firebase par lage asal rules (malik ne paste kiye; stockCount wali ijazat unhi mein thi, repo file purani thi).
- Nayi: saleStaff() (scope 'sale'), blueAccess/saleConfig get, posStock read: + saleStaff + blueStaff,
  stockCount match repo mein bhi, owner list mein stockCount.
- appSales/{saleId}: read = malik / full mulazim / sale mulazim.
  create sirf status 'new', aaj ki date, role == (malik ? 'owner' : 'staff'), by == uid, 1-150 lines, total > 0.
  update sirf printReq (dobara print ka hukam). Status/saleNo sirf PC (admin key) likhta hai.

App:
- sale.js (naya): view 'sale' (nav "🧾 Sale"; malik, full mulazim, sale mulazim).
  Counter (default) = R rate; Wholesale = W rate (wrate, warna R). Godam buttons (default branch 1).
  Item: upar search box (naam/code; Enter = barcode/akela item), 📷 camera, USB scanner (pos-stock ka detector sale screen par
  setSaleScanHook se yahan aata hai). Line: Ctn + Pcs + Rate. Rate sirf malik badal sakta hai (✎), mulazim readonly.
  Cash khud total; counter mein kam cash par save nahi; wholesale mein baqi = udhaar ("whole sale" khate mein).
  Draft localStorage 'sam-sale-draft' (aaj ka). Save -> appSales doc (id 's'+time+random), 4 sec baad bill saaf;
  server mana kare to bill wapas screen par + alert.
  "📋 Aaj ki app sales" dialog: status (intezar / Sale No / masla), line tafseel, "Dobara print" (printReq).
- pos-stock.js: saleStock() export (posStock listener bina ginti ke), setSaleScanHook, openSaleCamera;
  start(withCounts) — sale-only mulazim ke liye stockCount listener nahi lagta.
  USB scanner ab sirf Stock screen (.stock-head) ya Sale screen ([data-sale-root]) par pakadta hai.
- Login scope 'sale': auth-controller.js, cloud.js (saleConfig/salePrivate, salt 'noor-traders-blue-sale-v1', key 'sale-'),
  index.html (Sirf Sale), app.js (saleOnly, route, nav, title "Nayi Sale", PDF button chhupa), mulazim password form.
- cloud.js: listenAppSales(date), saveAppSale(doc), reprintAppSale(id).
- style.css: .sale-* classes.

PC (C:\khata-sync):
- sale-post.js v1 (sale-auto.bat loop, sale-log.txt; Startup folder mein sale-chupa.vbs; port 47815 = ek hi chale, exit 3).
  Listener: appSales status in [new, posting]; printReq > 0.
  Transaction se 'posting' -> POS: usp_Sale_InsertUpdate (Description = "BK-APP <id>" nishan, DocStatusID 1, SPID 1,
  CreatedBy 1, CCNo/BiltyNo = total, vNetAmount = total, COGS = qty x ItemBranchRate.PurchaseRate) + usp_SaleDetail_InsertUpdate
  (Qty PIECES, GBranchID = godam). Counter: Defaults.DefCustomerID, IsCreditSale 0, CashReceived = total.
  Wholesale: SaleAllowedParty mein "whole sale" party (PartyID 21629), IsCreditSale 1, CashReceived 0,
  cash > 0 ho to CRV (usp_Voucher_InsertUpdate: VoucherType/DocumentType 'CRV', DocumentNo = SaleID; Cash a/c Dr, party a/c Cr,
  "Sale No: X") — POS isi tarah CRV banata hai aur sale dobara save par usay mitata hai.
  Staff role: rate hamesha POS ka (R ya SaleRate3/SaleRateSize). Owner: app ka rate.
  Pehle "BK-APP <id>" wali sale mile to dobara nahi banti (done likh deti hai).
  Done: saleNo, saleId, crvNo, total, cash, doneAt. Masla: status 'failed' + error.
  Rasid: SQL se parh kar (notepad /p, 32 chaurai). node sale-post.js --print <SaleNo> = POS ki kisi bhi sale ki rasid.
  Stock kam ho to sirf log ("Stock kam") — sale nahi rukti (POS ka IsLockSaleStock abhi app par nahi lagaya).
- Taala (trg_SaleAllowedParty) waisa hi hai.

Baqi / aage:
- Sale date: abhi PC ka waqt (POS Defaults.SaleDate / IsUseSaleDate istemal nahi kiya).
- Barcode ki qty (ItemSubCode.SBItemQty, maslan 0.5) abhi istemal nahi — scan par 1 pc judta hai.
- 'failed' sale dobara bhejne ka button nahi (nayi sale banayein).
- Purchase bill qty/rate (pieces) aur cancelled bill ka masla abhi baqi (bill-dekho.json ka intezar).

==============================================================
2026-09-17 raat — App mein "(purana)" khate (masla, abhi hal nahi)
==============================================================
- pos-milan.js --customers-new ne POS ke purane customer khate zero + band kiye aur naam ke aage " (purana)" lagaya.
- Yeh naam app mein bhi aa gaye (pos-khata-sync POS->app naam sync, jab app account purane POS party se jura ho).
  Malik ke mutabiq yeh khate app mein nahi aane chahiye the; baqi sab theek synced hain.
- Kuch ke app baqaye bade hain: haseeb cus (purana) 2,593,012 lene; hbl noor traders (purana) 804,325 lene
  (hbl noor traders cash/bank khane ki list mein bhi aata hai). Kai 0 wale: HAFIZ HOTEL, ijaz wald chowdry..., mia arif sahb...
- Hidayat: app se DELETE na karein (pos-khata-sync POS party bhi hata deta hai).
- Jaanch: purana-dekho.js (sirf parhta hai) -> purana-dekho.json/.csv: har "(purana)" app khata: app baqaya, entries,
  posLinks, POS party (chalu/band, baqaya), aur isi naam ka doosra (asal) app khata.

==============================================================
2026-09-17 raat — v1.47.0: Din band (day closing) + mulazim ko poori ijazat (aaj ke din)
==============================================================
Malik ka faisla:
- Mulazim (full) AAJ ke din sab kuch kar sakta hai: entry banana, edit, delete, khate ka naam/phone/category.
- Purani tareekh: sirf malik (mulazim dekh sakta hai, badal nahi sakta — malik ne "dekhe, badal na sake" chuna).
- "Din band" button: mulazim ya malik daba sakta hai. Band hote hi mulazim sirf dekh sakta hai.
  Malik par koi rok nahi aur wahi dobara khol sakta hai (mulazim khol nahi sakta). Agla din khud khulta hai.
- Purchase mulazim par din band ka koi asar nahi.

Firestore rules (v1.47):
- todayStr() + dayOpen(): blueAccess/dayLock doc ka closed == aaj ho to mulazim ke saare likhne wale rule band
  (entry create/update/soft-delete, purchase entry ka blueStaff hissa, closing, custody, party/expenseAccount edit).
  exists() pehle — doc na ho to din khula (extra read nahi).
- blueAccess/dayLock: blueStaff sirf {closed: aaj, by, at} likh sakta hai (khol nahi sakta). Malik generic owner rule se sab.
- blueAccess/{id} get mein 'dayLock' bhi.
- NAYA: staffEditEntry() — mulazim aaj ki entry ke kind, partyId, amount, date(aaj), note, account, dailyIncluded,
  photos, deleted waghera badal sakta hai (rev+1, updatedBy = uid). Transfer entries phir bhi sirf malik.
- NAYA: party/expenseAccount ke name, phone, category, note mulazim badal sakta hai (opening sirf malik).

App:
- index.html: #dayLockBtn + #dayLockNote (syncDetails ke neeche).
- app.js: dayLock state + watchDayLock() (cloud.listenDayLock), paintDayLock() har render par,
  toggleDayLock() (confirm ke saath), lockedNow() = din band + staff full.
  Rok: capture-phase 'submit' aur 'click' handlers (data-sheet/data-count forms, data-del/edit/remove,
  transfer edit/delete, pos-done, edit-purchase, ready) + body.day-locked CSS se form chhup jate hain.
- cloud.js: listenDayLock(next,fail), setDayLock(day|'').
- style.css: .day-lock button, #dayLockNote, body.day-locked.
- Sale screen (appSales) par din band ka asar nahi — agar chahiye to rules mein appSales create par dayOpen() lagana hoga.

==============================================================
2026-09-17 raat — khata-dekho.js (CMD): POS chalu/band khate + app se milan
==============================================================
- Sirf parhta hai. node khata-dekho.js [--farq | --band | --chalu | --supplier | --sab | --dhoondo <naam>]
- POS: Party (customer/supplier) + Account.IsActive (chalu/BAND) + baqaya (VoucherDetail Debit-Credit, status 3 chhor kar)
  + sales ki ginti + aakhri voucher ki tareekh.
- App: blueKhata parties + entries se baqaya (opening + credit/payment - borrow/collection) / 100.
- Jora: id "pos-party-<id>" -> posLinks party:<appId> -> naam.
- Farq = app - POS. Khulasa: chalu/BAND ginti, dono/sirf app/sirf POS, kul baqaye, farq wale khate,
  aur KHABARDAR: BAND khaton mein app ka baqaya (purana band wala masla).
- Har dafa khata-dekho.csv banti hai.

==============================================================
2026-09-17 raat — khata-chalu.js: BAND (deactive) khate chalu karna
==============================================================
- Default: sirf list + khata-chalu.csv (BAND khata, baqaya, sales, aur usi naam ka naya CHALU khata mojood hai ya nahi).
- node khata-chalu.js --yes            -> saare BAND khate Account.IsActive = 1 (naam waisa hi, baqaya nahi chhoota)
- node khata-chalu.js --yes --naam-bhi -> upar wala + naam/PartyName se " (purana)" hata deta hai
- node khata-chalu.js --band-karo "naam" --yes -> wapas band
- Khabardar: pos-milan ne purane customers ka baqaya JV se zero kiya tha aur app ke customers ke naye POS khate banaye.
  Sirf chalu karne se POS list mein do khate (purana 0 + naya baqaya wala) nazar aayenge. Baqaya wapas karna (pos-milan --undo)
  ka matlab dohra hisaab — malik ko mana kiya gaya.

==============================================================
2026-09-18 — v1.47.1: mulazim ko Edit ka button bhi
==============================================================
- v1.47.0 mein rules ne mulazim ko aaj ki entry edit karne di, lekin app mein "Edit" button sirf owner() ko dikhta tha
  (3 jagah: khata list, entry card, daily list).
- Naya helper canEditEntry(e): malik hamesha; mulazim (full) tab jab entry aaj ki ho, deleted/transfer na ho, aur din band na ho.
- Teenon jagah owner()? ki jagah canEditEntry(e)? laga diya. Edit ka click handler pehle se sab ke liye khula tha.

==============================================================
2026-09-18 — v1.48.0: Khata BAND (deactive) + Khata DELETE (sirf malik)
==============================================================
Malik ka faisla: baqaya 0 ho magar entries mojood hon to khata delete na ho — "band" (deactive) ho jaye,
band khaton ka alag tab ho, un ki entries baad mein edit/delete na hon, aur delete sirf malik kar sake.
- Party doc mein naya field: active:false (band). Sirf malik likh sakta hai (staff party-edit rule mein 'active' nahi hai).
- app.js: parties() ab sirf active!==false; naya bandParties(). Khata list aur report mein tab 'band'.
  index.html: nav tabs mein "Band khate".
- accountForm (sirf malik): "🚫 Khata band karein" / "🔓 Khata chalu karein" aur "🗑 Khata delete".
  Delete sirf jab baqaya 0 ho (warna alert: pehle wasool ya transfer karein); confirm mein entries ki ginti aur
  POS wali baat batai jati hai. Delete = deleted:true (pos-khata-sync POS party hatane ki koshish karega).
- Band khate ki entries: canEditEntry / canDeleteEntry ab false (malik ke ilawa) — bandParty(e) check.
- Dialog ke buttons ke liye alag click listener (main wala handler #dialog tak nahi pohnchta).
- Band khate entry ke har picker se bhi gayab (expenseChoices, partyChoices, supplier, dasti — sab parties() par hain).
- Abhi baqi: POS taraf band karna (Account.IsActive=0) app se nahi hota — khata-chalu.js / POS se karna hoga.

==============================================================
2026-09-18 — v1.49.0: Stock ginti — "+ Jama", ⏳ Baqi / 🔍 Check nishan, Counting ON/OFF
==============================================================
Malik ka masla: ek item (dal chana) shop mein bhi, neeche bhi — do jagah ki ginti jama honi chahiye; larka bhoole nahi;
kuch items sirf "check" karne hote hain; aur malik ek button se counting band kar sake.
- "+ Jama" (Save ke saath, sirf jab pehli ginti ho): likhi qty PEHLI ginti mein jud jati hai. Prompt mein jagah ka note
  (ikhtiyari). History line: "+2 CTN + 0 PCS (neeche) → 5 CTN + 0 PCS". Count doc same (ctn/pcs/total), history entry mein add:{ctn,pcs,total}, note.
  Jama ke baad "baqi" nishan khud hat jata hai.
- Nishan stockCount/_flags {items:{<itemId>:{baqi,check,checkedAt,checkedNote}}} (setDoc merge). Mulazim bhi laga sakta hai.
  Row par buttons: "⏳ Baqi — aur ginna hai", "🔍 Check karo", aur check laga ho to "✓ Check ho gaya" (note prompt, checkedAt).
  Filters: "⏳ Baqi nishan (N)", "🔍 Check wale (N)".
- Counting ON/OFF: stockCount/_lock {off:true/false}, sirf malik (header button 🟢/🔴). OFF ho to mulazim ko ginti ke khane nahi
  dikhte (sirf ginti/farq + nishan buttons), Save/+Jama/Sab save ruk jata hai; malik khud gin sakta hai.
- Rules (v1.49) stockCount: read = owner/full/stock. write: owner sab; mulazim sirf non-"_" docs aur _lock.off != true;
  mulazim _flags likh sakta hai (keys items, updatedAt) chahe OFF ho.
- cloud.js: setStockFlag(id,f), setStockLock(off).
- PC post-farq.js: counts where('round'==) se aate hain — _flags/_lock par asar nahi.

==============================================================
2026-09-18 — v1.50.0: Mulazim ko account edit + Dasti, aur khate ki picture
==============================================================
- canEditAccount() = malik, ya full mulazim (din khula ho). Account edit (naam/mobile/qism/dasti) ab mulazim bhi;
  opening baqaya, POS sync, transfer, reminder, band/delete sirf malik.
- Khata screen: "Dasti mein shamil karein" aur "📷 Picture lagayein" ab canEditAccount() par (pehle owner()).
- Rules: staff party-edit keys mein 'dasti' aur 'hasPhoto' shamil.
- Picture: alag collection partyPhotos/{partyId} {photo (jpeg data URL, 400px, <=60KB), updatedAt, by}.
  Party doc par sirf hasPhoto:true/false. Picture sirf khata kholne par getDoc se aati hai (partyPhotoCache).
  cloud.js: getPartyPhoto(id), setPartyPhoto(id, photo|null -> deleteDoc). packPhotos(files, maxPx, maxLen) params.
  Rules partyPhotos: read owner/full; create/update keys photo/updatedAt/by, photo <= 90000 chars; delete owner/full (dayOpen).

2026-09-18 — v1.50.1: mulazim ke phone par khali screen (purani index.html cache)
- Masla: naya app.js + purana cached index.html -> $('saleNav')/$('dayLockBtn') null -> TypeError -> app khali.
- Fix: nav buttons ki wiring ab guard ke saath: if($('saleNav'))... (stockNav, dayLockBtn, saleNav aur baqi nav ids bhi).
- Phone par purana cache hatane ke liye: app kholein -> ↻ Sync/Update, ya browser ka site data clear.

2026-09-18 raat — v1.50.2: "Connection load nahi hua" (Brave / storage blocked)
- Repo sahi tha (https://github.com/samiullah-878/Noor-traders- , app https://samiullah-878.github.io/Noor-traders-/).
- Wajah: cloud.js connect() mein setPersistence + persistentLocalCache (IndexedDB) bina try/catch; Brave shields /
  data clear ke baad storage na mile to poora connect fail -> login screen par "Connection load nahi hua".
- Fix: dono try/catch; storage na mile to memoryLocalCache() (app chalti hai, offline cache nahi).
- v1.50.3: login screen par ab asal error bhi dikhta hai: "Connection load nahi hua: <code/message>" (masla dhoondne ke liye).

2026-09-18 raat — v1.50.4: ASAL MASLA — "connected is not defined"
- v1.47 se jo naye module-level functions likhe (watchDayLock, toggleDayLock, partyPhotoHTML/Form) un mein `connected`
  istemal hua, jo sirf import('./cloud.js').then(...) ke andar ka const hai. Module scope mein alias `cloud` hai.
  watchDayLock() boot par chalta tha -> ReferenceError -> catch -> "Connection load nahi hua" (sab ke liye).
- Fix: module scope mein har `connected` -> `cloud`.

2026-09-18 raat — v1.51.0: 🗑 Deleted entries (recycle bin)
- Entries pehle se soft-delete hain (deleted:true, indexData unhein chhor deta hai).
- Settings (⚙) mein malik ke liye naya button "🗑 Deleted entries (wapas layein)" -> trashView():
  saari deleted entries (search naam/note/raqam, tareekh-se filter, 300 tak, delete ke waqt ke hisaab se),
  har ek par "↩ Wapas layein" = save({...r, deleted:false}, r). Transfer entries par restore band (dono taraf ki hoti hain).
- Rules mein tabdeeli nahi (owner generic update).
- Yaad rahe: POS se jura entry wapas aane par pos-khata-sync POS mein voucher dobara bana dega.

2026-09-18 raat — v1.51.1: Stock ginti — live farq, "Kul PCS" khana hata, Check ka masla
- Masla 1: "✓ Check ho gaya" aur "+ Jama" prompt() istemal karte the — Android par installed PWA (home screen) mein
  window.prompt() null deta hai, is liye kuch nahi hota tha. Ab Check bina note ke foran lagta hai (checkedAt);
  + Jama confirm() se (note nahi).
- Masla 2: Ctn/Pcs likhte hi neeche line: "250 PCS · System -1,904.71 · Farq +2,154.71 (zyada) · Rs …" (liveCount, input event).
  Save waisa hi.
- "Kul PCS" wala teesra khana hata diya (readCount abhi bhi data-count-tot ko sambhalta hai, bas input nahi hai).
- v1.51.2: malik ne saaf kiya — "Kul PCS" khana WAPAS (rakhna hai), "+ Jama" button HATA diya (code saveCount(...,true) mein hai, button nahi).

2026-09-18 raat — v1.51.3: camera barcode scan tez
- Pehle: har 180ms ek detect, kamyabi par 900ms ruk, 720p, koi focus/zoom nahi.
- Ab: requestAnimationFrame loop (detect khatam hote hi agla), kamyabi par 350ms, na milne par 700ms,
  1080p@30, focusMode continuous, zoom ka button (default 2x agar mile) aur torch (🔦) ka button (.scan-tools).
  Same code dobara 1.5s (pehle 2.5s). closeScanner cancelAnimationFrame bhi.

2026-09-18 raat — v1.52.0: Nayi Sale — line par godam, Pcs pehle, camera button
- sale.js: har line mein "Godam" select (data-sale-lg); line.godam default bill ka godam. Stock/rate us godam ke item se (itemIn).
  Godam badalne par rate (agar edit na kiya ho) us godam ka. Save mein har line ke saath godam jata hai.
- Inputs ki tarteeb ab: Pcs -> Ctn -> Rate -> Godam. focusLast Pcs par.
- Camera: header mein "📷 Barcode scan karein" pehle se tha; ab neeche actions bar mein bhi 📷 button (bill khula ho to).
- PC sale-post.js v1.1: har line ka GBranchID = l.godam (warna bill ka godam); rate/cost bhi usi godam ki ItemBranchRate se.
  LAGAO: sale-post.js C:\khata-sync mein copy karke sale-post band/dobara (sale-auto.bat khud chala deta hai).
- v1.52.1: scanner poori screen (portrait, video 62vh, object-fit cover), default zoom 1x (auto 2x hata), naya "🎯 Focus" button
  (single-shot -> continuous) aur video par tap = focus. Hidayat msg: 12-15 cm door, seedha, light sirf andhere mein.

2026-09-19 — v1.52.2: scanner ka lens
- Masla: phone ke apne camera se photo saaf, app (getUserMedia facingMode environment) se dhundli — browser ne
  wide/macro lens chun liya tha (fixed focus).
- pickCamera(): enumerateDevices -> back/rear label, wide|ultra|macro|tele|depth chhor kar main lens (deviceId exact).
  Pehle ek dafa ijazat le kar labels lete hain. localStorage 'sam-scan-cam' mein chuna hua camera yaad.
- "🔄 Camera (N)" button: agla lens, scanner dobara khulta hai. Message mein camera ka label.
- Constraints mein advanced focusMode continuous bhi.

2026-09-19 — v1.53.0: camera scanner par tadad ke buttons (keyboard ke baghair)
- Scanner overlay mein .scan-pad: Pcs/Ctn switch + 1 2 3 6 12 24 + "+1". Aakhri scan hua item (lastScan) par lagti hai.
  scanQty Map(itemId -> {pcs, ctn}); pehli scan par pcs 1.
- Sale: setSaleQtyHook -> sale.js cart line ke pcs/ctn set (absolute), rerender (scanner khula rehta hai).
- Stock ginti: "Ho gaya" par scanQty se Ctn/Pcs (ya Kul) ke khane bhar diye jate hain + liveCount; phir Save khud karna hai.

==============================================================
2026-09-19 subah — PC: sale-post v1.2, post-farq v11, tools, SETUP/BACKUP
==============================================================
- Malik ne bataya: POS ka print service naya bill khud chhapta hai (barcode wali asal rasid). Is liye
  sale-post.js v1.2 aur post-farq.js v11 mein AUTO_PRINT = false (apni saadi rasid band). --print <SaleNo> ab bhi kaam karta hai.
- sale-post v1.2: bill ke saare items EK query (ItemID IN ... AND BranchID IN ...) -> tez. Line ka godam (v1.1) shamil.
- subcode-dekho.js: ItemSubCode ke khane/misalein/procs -> subcode-dekho.json (sub-barcode add feature ke liye).
- reorder-data.js [YYYY-MM-DD]: 1 Dec se items/stock/sales/returns/purchases/ledger CSV -> reorder-data.zip (reorder level ke hisaab ke liye).
  Har query alag try/catch; column naam ghalat hon to .error.txt banti hai + ledger-columns.csv se asal naam.
- SETUP.bat (Run as admin): Node (folder ka node-*.msi / winget / nodejs.org), npm install, SQL server/password poochh kar
  sync-stock.js mein likhna, SQL+Firebase test, *.vbs Startup mein, *-auto.bat chalu. Isi PC par bhi chala sakte hain.
- BACKUP.bat: Desktop par khata-sync-backup-<date>.zip (node_modules/logs ke baghair; firebase-key.json shamil).
- LAGAO.bat: sab kuch C:\khata-sync mein, backup *.v11old/v10old, sale-post/post-farq restart, logs.
Aage: cashier "post" screen ka intezar (bill scan -> paid), sub-barcode add, reorder report, Telegram din ka khulasa, WhatsApp ek-tap.

2026-09-19 — Print: seedha printer ko, TM-T88IV
- sale-post.js / post-farq.js: printText() ab notepad ki bajaye PowerShell Out-Printer -Name se seedha printer ko (koi window nahi).
  PRINTER_NAME = 'TM-T88IV' (Devices and Printers mein jo naam hai; ghalat ho to top ki const badal dein).
  AUTO_PRINT = true dono mein (yeh meri "saadi" rasid hai — POS ki Crystal Report wali nahi).
- Malik chahta hai POS ki Crystal Report rasid (jo post-farq/stock bill par khud niklti hai) sale-post wale bill par bhi khud nikle.
  Wajah: PC par POS ka apna print service chal raha hai jo naye SQL bills dekh kar khud print karta hai, lekin sale-post
  wale bill (BK-APP marker) ko pakarta nahi lagta. Abhi tak maloom nahi kaunsa process/service yeh karta hai —
  Task Manager > Details dekh kar pata karna hai, phir sale-post ka bill usi tareeqe se banana hai ke woh service use bhi pakre.

==============================================================
2026-09-19 (baad dopahar) — Print: printer ka naam seedha, Crystal Report ka mansooba
==============================================================
- sale-post.js / post-farq.js: PRINTER_NAME = 'TM-T88IV' (Devices and Printers ka naam). printText() ab notepad
  ki jagah PowerShell "Get-Content | Out-Printer -Name" seedha printer ko — tez, koi window nahi khulti.
  AUTO_PRINT = true dono mein (apni saadi rasid ab chalti hai; malik ne kaha stock ke bill par bhi asal mein
  meri hi text-rasid nikalti thi, POS khud print nahi karta).
- Malik chahta hai POS ki ASAL Crystal Report rasid (jo Thermal F5 se nikalti hai) khud-ba-khud chhape.
  Yeh sirf UI Automation se mumkin hai: script POS ki Sale screen khud khole (bill number daal kar) aur
  "Thermal F5" / Print button khud dabaye. Pehla qadam: pos-uia-dekho.ps1 (sirf dekhta hai, kuch nahi dabata) —
  POS mein koi bill khol kar chalana hai, us se Sale window ke saare buttons/AutomationId milenge
  (pos-uia-dekho.txt). Us ke baad asal "auto-print via POS" script banegi.
  Khatra jo malik ko bataya: POS ki window thori der screen par khul/band hogi; POS update hone par ID badal sakte hain.
- LAGAO2.bat: sale-post.js, post-farq.js, pos-uia-dekho.ps1 lagata hai (backup .v12old/.v11old).

==============================================================
2026-09-19 — v1.54.0: Sale screen — godam bar hata, camera search ke saath, tadad ka pad, POS jaisi tarteeb
==============================================================
- Godam ke buttons (Maal kis godam se) Sale screen se hata diye. Bill main branch (SALE_BRANCH) se; kisi line ka
  godam line ke andar wale "Godam" select se badla ja sakta hai (v1.52 se).
- Search results ab POS ki tarah CODE ke hisaab se tarteeb mein (adad wale code number ki tarah, warna harf ke hisaab se).
- List ke upar chhoti qatar: "📷 Scan" button + hidayat (bara button hata). actions bar ka 📷 waise hi hai.
- Item search se ya Enter se chunne par ab "tadad ka pad" khulta hai (saleModal): Pcs 1/2/3/6/12/24/+1,
  aur pack>1 ho to Ctn 1/2/3/5/10/+1, "✓ Theek hai" / "✕ Hatao". Keyboard kholne ki zaroorat nahi
  (scanner wale pad jaisa, jo pos-stock.js mein hai).

==============================================================
2026-09-19 shaam — Rasid: POS jaisi apni rasid (Crystal chhor diya)
==============================================================
- Crystal (rptSaleThermal.rpt) se print karne ki koshish chhor di (32-bit + dataset + purana SQL ke masle).
  Malik ne kaha: bas isi shakal ki saadi rasid har sale/stock bill par nikal aaye.
- sale-post.js: receiptFor() ab POS jaisi rasid banata hai — bara shop naam + phone, Sale No + CODE128 BARCODE,
  tareekh/waqt, Customer + area, jadwal (Items/PCS/CTN/Rate/Amount), Total (PCS/CTN), bara Rs total,
  Cash Received, Balance, aur neeche SHOP.lines (rabta, Bank HBL, IBAN, Accounts title, delivery time).
  Labour ka khana malik ne chhorne ko kaha.
- printText(): ab RAW ESC/POS — PowerShell + winspool (RawP class) se seedha printer ko bytes; is se barcode aur
  bara text chhapta hai. Files: sale-print.bin / raw-print.ps1 (farq: farq-print.bin / farq-raw-print.ps1).
- post-farq.js: farq bill ki rasid par bhi bara naam + Sale No ka barcode + paper cut.
- SHOP object sale-post.js ke upar hai — naam/phone/lines wahan badal sakte hain.

==============================================================
2026-09-19 shaam — v1.55.0: camera ki screen se poora bill
==============================================================
- Scanner overlay ab bill banane ki screen: upar camera, phir chhoti tools qatar (Focus/zoom/Camera/Light),
  us ke neeche SEARCH bar (naam/code se item add — saleFindHook), phir fix qty row: Pcs/Ctn + 1..12 + "+1"
  (hamesha AAKHRI item par lagti hai), phir items ki list: naam + qty x rate = amount, aur neeche "Kul: Rs …".
- pos-stock.js: scanOrder/scanItems (camera ki lines), lineOf()/drawNames() nayi shakal mein,
  setSaleFindHook(fn) naya export; saleHook(code, direct) ab seedha item bhi le leta hai (search se chuna hua).
- sale.js: setSaleFindHook lagaya (stock ke items mein naam/code/barcode se dhoondta hai), scan hook direct item leta hai.
- style.css: .scan-find/.scan-hits/.scan-sum/.scan-names nayi lines, tools ke buttons chhote.

==============================================================
2026-09-19 raat — v1.56.0: scanner ki ghalat parhai, search upar, chhote buttons, mulazim Dasti
==============================================================
- pos-stock.js scan loop: camera kabhi ek frame mein ghalat code parhta tha (misal 6928738109038 ko 3908742709038),
  item sahi add ho jata phir bhi upar "stock mein nahi mila" likha aa jata. Ab: mila hua code foran chalta hai;
  NA mila code sirf tab "nahi mila" dikhata hai jab wahi code 3 dafa lagataar aaye AUR pichhle 2.5 sec mein koi
  sahi item add na hua ho (badCode/badN/lastGoodAt). lastCode ab sirf sahi scan par set hota hai.
- Scanner layout: search (.scan-find) ab sab se UPAR, results (.scan-hits) uske neeche; video ab .scan-cam ke andar
  (laal line camera ke beech mein, pehle message ke upar aa jati thi); 1-12 / Pcs / Ctn buttons chhote (style.css v1.56 block).
- app.js Dasti Payment screen: mulazim (full access, din khula — canEditAccount) ab "Account Dasti mein shamil karein"
  aur "Dasti se nikalein" kar sakta hai. Reminder / payment transfer / naya account ab bhi sirf Malik.
  Firestore rules mein tabdeeli ki zaroorat nahi (staff pehle se party.dasti update kar sakta tha).
- Version 1.56.0 (index.html, sw.js cache, imports, version.json).

==============================================================
2026-09-19 raat — v1.57.0: scanner — awaz, kaala camera, list gayab
==============================================================
- beep(ok): WebAudio (audioCtx openScanner ke click par banta/resume hota hai). Sahi/"pehle se" scan = ek patli beep,
  "nahi mila" = do moti beep. Vibrate waisa hi.
- Kaala camera: dobara kholne par kuch Android phones tasveer nahi dete. play() ke 1.5 sec baad video.videoWidth 0 ho to
  closeScanner()+openScanner() khud (camRetry, 2 dafa tak; doosri dafa localStorage 'sam-scan-cam' bhi hata deta hai).
  Phir bhi na chale to message. finishScan() par camRetry = 0.
- List/tadad gayab (v1.56 layout ki ghalti): .scan-cam flex grow karta tha aur .scan-names (overflow:auto, min-height 0)
  0 tak sukarr jata tha. Ab .scan-inner > * flex-shrink:0, sirf .scan-cam flex:1 1 0 (min 140px), .scan-inner scroll.
  Dobara kholne par showPad() foran (aakhri item ki tadad).
- Agla kaam (files ka intezar): POS sub-barcode (ItemSubCode) ki tadad, jaise garam masala 0.25 label — scan par wohi
  tadad khud aaye. Chahiye: sync-stock.js + subcode-dekho.json.

==============================================================
2026-09-19 raat — v1.58.0 + PC sync-stock v5: sub-barcode ki tadad (garam masala 0.25 label)
==============================================================
- POS dbo.ItemSubCode (ItemSubCodeID, ItemID, SBBarCode, SBSaleRate, SBItemQty, IsShowOnLookup). 1885 rows, 142 ki
  SBItemQty 1 nahi (0.5, 0.25, 0.125, 0.05, 5, 0.33, 0.1, 0.025, 2, 0.2). SBSaleRate taqreeban sab 0 — istemal nahi kiya.
- PC: patch-subqty.js sync-stock.js mein subQtyMap() jorta hai (sirf SELECT) aur har item par bq: [{b, q}] bhejta hai
  (sirf q != 1). Backup sync-stock.v4old.js. Password/SQL_CONFIG ko nahi chherta. Pehli dafa saare chunks dobara likhe jate hain.
- App sale.js: subQty(it, code) scan kiye code ko item.bq se milata hai (aage ke zero bhi), addItem(it, q). Search se chuna = 1.
  Scan hook ab { state, item, pcs, ctn } (bill line ki asal tadad) lautata hai; pos-stock.js camera ki scanQty isi se set
  karta hai. Pehle ek item dobara scan karne par bill mein 2 ho jata tha magar camera ki list 1 dikhati thi — theek.

==============================================================
2026-09-19 raat — v1.59.0: camera screen par search + "Tadad" khana, camera phansne ka hal
==============================================================
- Scanner ke upar ek line: .scan-q (search) + .scan-n (Tadad, inputmode decimal). SALE mein: item chunne (tap ya Enter =
  pehla) par add NAHI hota — chosen = item, cursor Tadad mein; Tadad par Enter -> saleHook(code, item, qty) se us tadad
  (Pcs) ke saath add (khali/0 = 1), beep, search khali, cursor wapas search (keyboard band nahi). Pehle se list wala item
  jama hota hai (addItem). hitBox par pointerdown preventDefault (tap se keyboard band na ho). Stock ginti mode pehle jaisa.
- sale.js scan hook: (code, direct, qty) — qty > 0 ho to wohi, warna direct = 1, warna sub-barcode ki tadad (v1.58).
- Camera: gumT() getUserMedia par 4 sec timeout (der se mila stream foran band). Timeout par ek dafa khud dobara
  (saved lens bhool kar), phir message "camera kahin aur khula hai". document visibilitychange (hidden) par finishScan()
  — tab/app peeche jaye to camera chhor deta hai (6-7 tabs khule the, ek tab camera pakre rehta tha).

==============================================================
2026-09-19 raat — v1.60.0: Pcs/Ctn button, bari list, tez awaz
==============================================================
- Tadad khane ke saath .scan-u button: har naye item par Pcs; tap = Ctn (sirf pack > 1 wale item par, warna disabled).
  Ctn par saleHook ko qty * pack pieces jate hain; sale.js addItem ab NAYI line par bhi poore carton alag karta hai.
- beep: gain 0.6, 0.18 sec (nahi mila = 2 x 0.2 sec), har beep par audioCtx.resume(); scanBox par kisi bhi pointerdown
  (capture) se audioCtx bana/resume — kuch phones awaz tab tak nahi dete jab tak screen na chhui jaye.
- style.css v1.60: scan-names naam 19px mote, line 16px, max-height 36vh; pad ka naam 17px; Kul 18px; message 16px.

==============================================================
2026-09-19 raat — v1.61.0: POS jaisa piece/carton rate, kaala camera ki asal wajah, mulazim ka payment transfer
==============================================================
- KAALA CAMERA / LIST GAYAB ki asal wajah: pos-stock.js mein r2() maujood hi nahi tha (v1.55 ki lineOf ise istemal
  karti thi). Ek bhi item hone par drawNames() ReferenceError deta, openScanner wahin ruk jata -> camera kabhi chalu
  nahi hota. Ab r2 top par defined.
- RATE: POS Items/ItemBranchRate: SaleRate = carton ka rate fi piece ("Cotton Rate" = SaleRate x PackQty, olivia
  233.333 x 12 = 2800), SaleRate2 = khule piece ka "Peice Rate" (olivia 260). App/sale-post pehle sirf SaleRate lete the.
  * PC sync-stock v6 (patch-rate2.js): item.rate2 = ItemBranchRate.SaleRate2 (sirf > 0 ho to).
  * sale.js: counter mein line.rate = rate2 || rate (piece), line.crate = rate (carton fi piece); lineTotal =
    ctn*pack*crate + pcs*rate. Wholesale ek hi rate (wrate). Malik rate badle to crate bhi wohi. Line mein "CTN rate X".
    Save: carton aur piece ke rate alag hon to do lines {unit:'ctn'} + {unit:'pcs'}; sirf ek ho to wohi unit.
  * pos-stock lineOf (camera ki list) bhi isi hisaab se.
  * PC sale-post.js v1.5 (v1.3 wali text rasid par bani, image rasid NAHI): mulazim ki counter line unit 'ctn' ->
    SaleRate, 'pcs' -> SaleRate2||SaleRate, unit na ho (purani app) -> pack se taqseem. Malik ki line app ke rate se.
- MULAZIM TRANSFER: party ke khate mein canEditAccount() par "⇄ Payment transfer" (sirf naya, tareekh aaj, readonly).
  commitAccountTransfer / cloud.saveTransfer mulazim ko sirf rev 0 naya (edit/delete malik). firestore.rules: blueKhata
  mein staff create: blueStaff && dayOpen && validTransfer && isToday(date) && rev == 1 && by/updatedBy == uid.
  RULES FIREBASE CONSOLE MEIN PUBLISH KARNI HAIN (project note-traders-khata-7ccc1).

2026-09-19 raat — v1.61.1: TADAD 3 decimal (sub-barcode 0.125 pehle 0.13 ban jata tha)
- sale.js: r3() tadad ke liye (linePcs, ctnPcsOf, addItem jama, save lines qty); num() ab 3 decimal tak dikhata hai.
  Raqam/rate r2 hi. pos-stock.js: num 3 decimal, lineOf ki tadad 3 decimal.
- PC: patch-qty3.js (sync-stock v7: bq ki q 3 decimal), sale-post.js v1.5.1 (qty r3, rasid par tadad 3 decimal).

2026-09-19 raat — v1.61.2: camera ki list bill ke saath
- Masla: "Naya bill" / item ✕ ke baad bhi camera screen purani list aur purana Kul dikhati thi (scanOrder/scanItems/
  scanQty pos-stock.js mein alag yaad rehte the).
- pos-stock.js: setSaleCartHook(fn) + syncFromCart() — openScanner (sale) har dafa list bill (cart) se dobara banata hai.
  sale.js hook: cart ki har line -> { item: {id, name, pack, cName, uName, rate: crateOf(l), rate2: l.rate}, pcs, ctn }
  (camera ki raqam bill ke asal rate se, malik ka badla rate bhi).

2026-09-19 raat — v1.62.0: har add bill mein NAYI line (malik: "jaise jaise add ho ek ek item aaye")
- sale.js addItem() ab kabhi jama nahi karta: har scan / search / Enter par nayi cart line, har line ki pehchan l.k
  (newKey). addItem line lautata hai; scan hook { state, item, line: k, pcs, ctn }. qtyPad(cart.length - 1).
  setSaleQtyHook(it, q, key): line k se (na mile to us item ki aakhri line). setSaleCartHook har line ka key deta hai.
- pos-stock.js camera: scanOrder / scanItems / scanQty ab LINE key se (sale mein), stock-ginti mode mein pehle ki tarah
  item id se. lastKey = aakhri line; pad (1..12, Pcs/Ctn) usi line par; list mein har line alag, "now" = lastKey.
  syncFromCart ke baad lastKey = aakhri line.
- Wahi barcode camera ke saamne ruka rahe to lastCodeAt har frame taaza — dobara tab ginta hai jab barcode 1.5 sec
  hat kar phir aaye (warna har 1.5 sec nayi line banti).
- POS / sale-post par koi tabdeeli nahi: ek item ki kai lines pehle se alag SaleDetail banti hain.

2026-09-19 raat — v1.63.0: camera screen teen hisson mein (malik ki farmaish)
- .scan-mid (flex row): LEFT .scan-left = camera tools (Focus / zoom / Camera badlein / Light) upar se neeche
  (tools ab msg.after ki jagah .scan-left mein append); BEECH .scan-cam; RIGHT .scan-pad = tadad ki patli patti
  (upar item ka naam, Pcs/Ctn, 1-12 do columns grid, +1 poori chaurai). Pad hidden ho to camera poori jagah le leta hai.
- Neeche message, items ki list, Kul, Ho gaya / Band karein pehle jaise. CSS: style.css ka v1.63 block.
- Headless Chromium (390px) par mock render karke layout dekha gaya.

2026-09-19 raat — v1.64.0: camera ki list mein har line ka laal ✕
- pos-stock.js: drawNames (sale) har li mein button.scan-del data-del = line key; namesBox click -> saleDelHook(key),
  scanOrder/scanItems/scanQty se bhi hataya; kati line par pad laga tha to lastKey = aakhri bachi line.
  setSaleDelHook(fn) naya export. Stock-ginti mode mein scanList se bhi hat jata hai.
- sale.js: setSaleDelHook(key) -> cart.findIndex(l.k === key) splice, rerender. Koi "pakka?" sawal nahi (malik: tez kaam).
- style.css v1.64: li padding-right, .scan-del 38px laal, right par beech mein.
- NOTE: pehle ke version-bump sed ne purane comments ke "v1.61.2" jaise nishan bhi badal diye the — code par asar nahi.

2026-09-19 raat — v1.65.0: camera poori chaurai, buttons upar tairte hain
- Malik ka sawal: chhota camera speed kam karta hai? Nahi — BarcodeDetector poora video frame parhta hai; screen par
  sirf dikhne wala hissa (object-fit: cover) kata hota hai. Masla sirf nishana lagane ka tha.
- style.css v1.65: .scan-mid position:relative (block); .scan-cam absolute inset 0 (poori chaurai); .scan-left
  (4 tools, 54px) aur .scan-pad (tadad, 84px) absolute, z-index 3, aadhe shaffaf; pad height content jitni
  (max 100%-10px, scroll) — pehle +1 neeche kat jata tha. Koi JS tabdeeli nahi.

2026-09-19 raat — v1.66.0: BARCODE LABEL print (PC ka TSC TTP-244 Pro)
- Stock tab: har item par "🏷️ Label" (rowCache) -> dialog: code, stock, piece / ctn / wholesale rate; har barcode
  (ItemCode + bc) ki line: tadad (bq, warna 1), label rate = tadad x (rate2 || rate), kitne label (1-200), 🖨️ Print.
- cloud.js: requestLabel(d) -> labelJobs/{id} {itemId, code, name, qty, rate, copies, status:'new', by, at};
  watchLabel(id, cb) -> status done / failed / skipped par message; 45 sec mein jawab na aaye to "PC on hai?".
- firestore.rules: match labelJobs — read + create malik / full / stock / sale mulazim (keys hasOnly, status new,
  by == uid, copies int 1-200). PC admin key se update karta hai. RULES PUBLISH KARNI HAIN.
- PC: label-print.js v1 (alag script, SQL nahi), label-auto.bat + label-auto.vbs (Startup). Dekhein AI_NOTES_label.txt.

2026-09-19 raat — v1.67.0: Stock screen saaf (malik ki farmaish)
- Shuru mein filter 'all' (Sab items) aur branch NOOR TRADERS (1) — pehle 'has' / pehli branch.
- Hataye: "Farq wale" aur "🔍 Check wale" filter, har item par "Check karo / Check ho gaya" buttons, samne se
  "Nayi ginti shuru" aur "Counting ON/OFF", aur Kul farq ki bari raqam.
- GINTI ab ek line (gintiLine): "DD-MM ki ginti: N items gine gaye · bill ban gaya / abhi nahi bana" + 📋 Dekhein +
  (malik) Farq ka bill PC par banao. postLine (PC preview / Bill banao / Cancel) aur bills history pehle jaise.
- 📋 Dekhein = openGinti(): is branch ke gine gaye items (naya pehle): gina (ctn+pcs), system, farq, Rs; kul farq.
  Malik ke liye khirki ke andar hi "Nayi ginti shuru" (bill ke baad zaroori, warna purani ginti chalti rehti).
- Counting OFF ho (_lock.off) to malik ko sirf "🔴 Counting band hai — kholein" dikhta hai (warna mulazim phans jata).
- startNewRound label ab "YYYY-MM-DD HH:MM" (ek din mein dobara shuru ho to purani ginti alag rahe). countId mein
  round nahi, PC post-farq round ko string ki tarah milata hai.

2026-09-19 raat — v1.68.0 + PC sync-stock v8: label mein sirf POS "Show" ✓ wale sub-barcode
- POS ItemSubCode.IsShowOnLookup = "Show" check (malik: check = chalu, bina check = band).
- PC patch-show.js: sync-stock showMap() (sirf SELECT) -> jis item ke sub-barcode hain us par bs: [Show ✓ wale]
  (khali array bhi). Backup sync-stock.v7old.js.
- pos-stock.js labelRows: r.bs ho to sirf asal code + bs; na ho (purani sync) to asal code + sab bc.
  SCAN par asar NAHI (bc waisa hi) — POS ke bohat se asal company barcode par Show nahi laga, wo scan hote rehne chahiye.
- Ginti "+ Jama" ki tadad ab 3 decimal tak (pehle 0.125 -> 0.13).
- Malik ka sawal (ginti do dafa save): aam "Save" pichhli ginti ki JAGAH leta hai (bill aakhri save se), "+ Jama" pichhli
  mein jorta hai. Har save par system stock bhi usi waqt ka likha jata hai; har save history mein rehta hai.
