import test from 'node:test';import assert from 'node:assert/strict';import {balance,daily,cents} from '../model.js';
test('linked payments/receipts update ledger and daily totals exactly once',()=>{const p={id:'a',opening:100000},rows=[{partyId:'a',kind:'collection',amount:30000,date:'2026-09-11'},{kind:'sale',amount:200000,date:'2026-09-11'},{kind:'expense',amount:10000,date:'2026-09-11'},{partyId:'b',kind:'payment',amount:5000,date:'2026-09-11'}];assert.equal(balance(p,rows),70000);assert.equal(balance({id:'b',opening:-10000},rows),-5000);const c=[{date:'2026-09-10',change:2000},{date:'2026-09-11',change:3000,cash:214000}];assert.equal(daily('2026-09-11',rows,c).difference,0);assert.equal(daily('2026-09-12',[],c).opening,3000);assert.equal(daily('2026-09-12',[],c).cash,0)});
test('credit/opening balances never inflate cash sales; money rounds to paise',()=>{assert.equal(daily('2026-09-11',[{date:'2026-09-11',kind:'credit',amount:10000}],[]).sale,0);assert.equal(cents('12.34'),1234);assert.throws(()=>cents(-1));assert.throws(()=>cents('NaN'))});
