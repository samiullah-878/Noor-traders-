V1.9 — Emergency recovery

Existing repository: use update-only ZIP; replace included files. Keep other folders. Sync/Update and reload. Verify v1.9.0.
New repository: extract COMPLETE ZIP and upload all app files with vendor folder. Enable GitHub Pages as before. All app paths are relative.

FIRST SAVE YOUR DATA:
On the same phone/browser where data was used, login page > Backup / Emergency local mode > Login ke baghair device backup.
This exports ONLY data available on that device (Firestore cache + saved recovery copy). It may be incomplete or stale. Export metadata states this. A new domain/browser has no old device cache. No available copy means another used device or existing JSON backup is needed.
Never clear browser site data to troubleshoot before downloading your backup.

TO WORK WHILE LOGIN IS BLOCKED:
Login page > Backup JSON se local kaam kholein > select downloaded JSON. This opens a separate local workspace; it does not authenticate to or change Firebase. The backup file itself gives access to its records, so keep it private.
Entries, purchases, closing and reports use this local copy. Local mode banner stays visible. Sync/Update in local mode downloads a backup instead of cloud syncing. Settings Backup also downloads your current records. After reload use Pichhla local kaam kholein.
Data is stored in this browser. Download backups regularly. Uploading files to a new repository does NOT move data automatically. Select JSON inside the app; DO NOT put your financial backup in a public GitHub repository.
Local changes do not automatically merge into cloud. Existing cloud restore refuses conflicting records; owner must reconcile these when cloud service returns.

LOGIN:
Username remains admin. For Malik select the actual existing owner email from the dropdown. Each click now makes one login attempt rather than trying all three emails. Last successful owner email is remembered. This does NOT reset Firebase throttling or change passwords. If Firebase says too many requests, avoid repeated attempts; recovery mode is available meanwhile.
Staff permission diagnostics and supplied rules remain from v1.8.

Limitations: server login block has not been cleared or verified in production. Device cache completeness is unknown until checked against your own records. Tests covered local persistence logic, edits, conflicts, backup validation, existing ledger flows and one-account login. Physical Android and your live Firebase remain untested.
