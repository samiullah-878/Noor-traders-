Apply over v1.15. Replace included files and use Sync / Update. Import verification reads now run concurrently, preserving revision and duplicate checks. Progress identifies reading, verification and saving. No live data, rules or Staff app changed. Production import speed depends on network and Firebase availability.